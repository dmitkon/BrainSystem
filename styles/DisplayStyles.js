import { StyleSheet } from 'react-native';

export const displayStyle = StyleSheet.create({
    display: {
        width: 300,
        height: 300,
        borderRadius: 300,
        alignItems: 'center',
        justifyContent: 'center'
    },

    text: {
        fontSize: 32,
        color: 'white',
        textAlign: 'center'
    }
});
